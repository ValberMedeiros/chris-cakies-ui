
import * as React from 'react';
import {Container, Typography} from "@material-ui/core";

export const Dashboard = () => {
    return (
        <Container>
            <Typography>
                Dashboard
            </Typography>
        </Container>
    );
};